document.addEventListener('DOMContentLoaded', function() {
    const galleryContainer = document.querySelector('.gallery-grid');
    galleryContainer.style.opacity = 1; // This line isn't strictly necessary due to CSS animation
});
